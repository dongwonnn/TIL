> 7월 29일 강의를 정리했습니다.
> 코드 리뷰 후 리팩토링 과정을 진행했습니다.

# 팀 미션 정리

이론, 정리글 보단 직접 사용해보고 장단점을 파악하는게 중요합니다 !

## 1. CRA Structure

직접 구조를 잡아보고 하나만 고집하지 않고 여러 구조를 사용하는 것이 중요합니다.
정답이 정해져 있지 않고 사용하던것이 나중엔 레거시 코드가 될 확률도 있습니다. 프로젝트를 설계전 충분한 회의를 거쳐 정해야합니다.

### 기본적으로 지켜야 할 원칙

정답은 없지만 꼭 지켜야할 원칙 !

> **한 파일은 하나의 기능을 합니다.**
> 유지보수, 확장성을 위해 **파일 이름만 봐도 이것의 기능이 무엇인지 알아야** 합니다.
> 더 나아가 깊숙한 구조라도 **이 기능이 어디에 위치하고 있는지**는 알 수 있도록 합니다.

## 2. CSS 작성 방식

SASS, styled-components, css-module등 여러가지가 있지만 어느 하나가 좋다라고 말할 수 없고 회사마다 다릅니다. 대표적인 sass와 styled-component의 장단점을 알아보겠습니다.

#### SASS

👍🏼 장점

- 문법적으로 훌룡합니다.

👎🏼 단점

- className을 신경쓴다 해도 전역 공간을 오염시켜 className 중복을 일으킬 수 있습니다.

#### styled-component

👍🏼 장점

- 유니크한 className을 지정해 전역 공간 오염을 막을 수 있습니다.

👎🏼 단점

- 태그가 아닌 컴포넌트 이름을 사용하면서 태그들의 구조를 한눈에 파악할 수 없습니다.

## 3. Commit Message

[이전 포스트](https://velog.io/@tunakim/POB03-%EC%BB%A4%EB%B0%8B-%EC%BB%A8%EB%B2%A4%EC%85%98-%EC%A0%95%EB%A6%AC)에서 작성한 Commit Convention을 따릅니다.

추가로 날짜나, 리뷰어가 작성자를 쉽게 알아보기 위해 이름을 쓰는 경우도 있습니다.

# 기업 과제 코드 리뷰

## HTML 무시 금지

div 태그 남발 금지
한 파일에 2개 컴포넌트 작성하지 말자

## CSS 공부 하기

어떻게든 돌아가도록 하자라는 식으로 하면 유지보수가 굉장히 하기 어려워집니다. CSS를 공부해 정확히 알고 사용해야 합니다.

#### 자주하는 실수들

1. 쓸모없는 property는 지웁니다. 예를들어 디폴트로 세로 정렬인데 굳이 flex-direction : column 할 필요 없습니다.
2. 자신의 모니터만 생각하면 안됩니다. 여백을 pixel 값으로 설정한다면 자신의 모니터에는 정 가운데 일지라도 다른 환경의 모니터에서는 그렇지 않습니다. ( margin: 0 auto로 설정 ! )
3. height 값 설정하지 않습니다. height는 내부 컨텐츠 자체의 height으로 설정되게 합니다. height를 설정한다면 컨텐츠에 한참 모자르거나 튀어나오는 상황이 발생할 수 있습니다.

#### Styled Component 관련 실수

1. styled component 안에 className을 사용하는경우, 스타일 컴포넌트를 다른 곳에서 재사용할 때 className이 전역 공간 오염을 일으킬 수 있습니다. 이것은 styled-components의 장점을 이용하지 않는 경우입니다. styled-component안에 태그를 쓰는 경우는 괜찮습니다.

## 유지보수 관련

### 상수 사용하기

API 주소와 같은 여러곳에서 사용하는 값들은 값이 변한다면 값을 사용하는 모든 곳을 찾아 일일이 바꿔야 하는 불편함이 있습니다. 이를 위해 상수화 시켜 한번에 관리하는 것 이좋습니다.

#### BASE_URL과 END_POINT

BASE_URL은 공통으로 사용하는 API 주소 까지, END_POINT는 뒤에 사용할 변수로 설정합니다.
예를 들어 댓글 정보 API 주소가 `https://dongwon.com/comment`, 유저 정보 API 주소가 `https://dongwon.com/user` 라면, `https://dongwon.com`까지가 BASE_URL입니다.

```javascript
export const BASE_URE = "https://dongwon.com/";
export const DEFAULT_LIMIT = 10;
```

## 게으른 개발자에게 좋은 코드가 나온다

절대경로로 사용할 수 있도록 ../ 없애기 -> 생산성 !!
import 도 순서 맞춰서

## Keywords

라이브러리르 쓰라는 건 아니었음.. 라이브러리 사용하지 말기

쓰로틀: 스크롤 이벤트를 할 때마다 수 많은 이벤트가 발생한다. 타이머를 걸어두고 일정시간동안 이벤트를 발생시킬 수 있다.
디바운스 : 계속 호출하도록 되어있지만 처음과 끝에만 실행하도록 할 수 있음. 구글 타이핑, 결제하기 버튼 연속 클릭,

#### intersection observer

- 문서를 항상 잘 봐야한다. -> ie 는 안된다.
  이런 경우 폴리필. 어느 브라우저에서도 구현될 수 있도록.
  항상 검색해서 ie에서 되는지 확인해야한다.
  caniuse 사이트에서 검색해보기 !
- ie는 꼭 고려해야하는가 ? -> 마케터가 테스트를 할 때 ie로 접속하는 사람이 너무 많다. -> 고려해야한다.

- 더이상 데이터가 없다 -> 처리하기. 사용자의 입장에서 로딩이 안뜨도록. 10보다 이하로 처리하기

#### React 쓰는 이유

생태계가 좋다. 개발자들이 쉽게 사용할 수 있도록 문서를 잘 만들었음.
프론트는 브라우저의 자원을 사용한다. -> 가장 만이 사용하는 자원은 돔을 조작할때 사용한다. 즉 돔을 조작을 줄여야한다.
react/vue는 실제 돔이 아니라 virtual dom을 사용. 바꼈다고 생각했을때 최종적으로 dom을 바꿔준다.

## utils 함수

- 함수, 로직을 모아놓은 함수. (formmating...)
- custom hook 같은 경우. 한 번이라도 재사용이 된다면 빼는 게 좋다.
- 주니어: 남의 것을 잘 사용해야한다.
- 미들레벨 : 내가 만든걸 남이 잘 쓰게 해야한다.
- 유닛 테스트. utils은 어떤 input이 들어갈지 모른다. 날짜를 string으로 들어왔는지, date 타입으로 들어왔는지 모른다. 하나하나 다 체크해야한다. -> unit test를 이용해서 체크한다.
- 이번 과제는 나눠서 할거임. 나눠서 cra에 +

## Client 리소스로 데이터 사용 ( mock data )

아직 back에 api를 못만들었을 때 어떻게 해야할까 ?

- json 이용
  - 프론트는 js를 하지만 백엔드는 여러가지 언어로 개발한다. 따라서 통일할 필요가 있었고 프론트는 js 사용하니 js 문법을 빌린 형식, 즉 json이 만들어졌다. 따라서 백엔드의 언어가 뭐든지 데이터를 보내면 json 형식에 맞춰 들어오게 된다.
- json에 사용가능한 타입들
  Stirng..
- 사용할 수 없는 걸듯..

- js와 다른점
  props안에 쌍따옴표를 넣어야한다. 변수 사용 불가 오로지 데이터 형식만 제공한다.
- 파싱과 직렬화
  1. stringify() : 쌍따옴표 없던 객체 조차도 json으로 변환해준다.
  2. parse() : json을 파싱해 자바스크립트 값으로 바꿔준다.
- localStorage

### public에 대해

index.html node_moduels에서 할 수 없는경우 index.html 안에 넣어야한다.
robots.txt : 검색 정보 관한 파일. 실제 서버에 들어가면 직접 접근할 수 있는 리소스들

json 통신하는 방법

1. data 폴더 안에 data.json에 fetch 함수에 데이터 통신을 할 수 있음.
2. json 파일 쉽게 import 할 수 있음.

### 간단한 데이터가 필요하다면 ?

data 형태로 만들기 data.js

comment.js에서 상수로 만들어서 import 해서 사용하면된다.

## 과제

기능 분담하기. 리덕스 전혀 필요 없음. 안써도 된다.
confilct 경험해보기

readme 에 어떤 기능을 구현했는지 간단히 작성
css는 사이트 보면서 비슷하게만 하면 된다.

1. 클래스 컴포넌트 사용해서 만들어야한다.
2. 세션, 로컬 스토리지 사용하기
3. 외부 api 사용 금지

넷플리파이로 배포하기

#

#

#

페어 플레이를 하며..
상대적으로 잘하는 사람은 면접 준비한다 생각하고 말을 많이 하자.
상대적으로 부족한 사람은 감사함 표시, 듣는거 내꺼로 만들기.
궁금한거 물어보고 공부 많이하기

리드미 : 트리구조 나열안해도됨. 처음 보는 사람도 이해할 수 있도록 설명하기. 큰거 위주로 한눈에 파악할 수 있도록. 자신 없으면 안써도 된다.

#

#

#

#

#

#

#

#

#

#

#

#

#

#

#

#

#
